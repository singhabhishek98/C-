#include<stdio.h>
int main()
{
    int n,k,x;
    scanf("%d",&n);
    scanf("%d",&k);
    int a[n];
    for(int i=0;i<n;i++)
    {
        x=(n+i-k)%n;
        scanf("%d",&a[x]);
    }
    for(int i=0;i<n;i++)
    {
        printf("%d ",a[i]);
    }
}
